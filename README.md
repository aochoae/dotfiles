# Dotfiles

Este repositorio contiene archivos de configuración para usar en Linux y macOS.

## Uso

Ejecuta el script de instalación desde la raíz del repositorio:

```bash
bash install.sh
```

El script copia los archivos de configuración al directorio de inicio del usuario y aplica la configuración adecuada según el sistema operativo.
